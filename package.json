{
  "name": "build_script",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "cd client && npm run test",
    "build": "npm install -g eslint-plugin-prettier && cd client && npm install && npm run build && cp -r build ../server",
    "install": "cd server && npm install && cd ../client && npm install",
    "start": "node server/index.js",
    "lint": "eslint ."
  }
}
